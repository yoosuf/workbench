# Workbench GraphQL API endpoint
WORKBENCH_API_URL=http://localhost:4000/graphql

# JWT obtained via the `login` mutation (same credentials as the Workbench web app):
#   mutation { login(input: { email: "you@example.com", password: "..." }) { accessToken } }
WORKBENCH_AUTH_TOKEN=
